[loggers]
keys=root

[handlers]
keys=console,file,syslog

[formatters]
keys=color,extended,simple

[logger_root]
level=NOTSET
handlers=console,file

[formatter_color]
class=helperlib.logging.ColorFormatter
format=[%(asctime)s] [TARGETS] [%(levelname)9s] %(message)s
datefmt=%H:%M:%S

[formatter_extended]
format=[%(asctime)s] [TARGETS] [%(levelname)9s] [%(name)20s] [%(filename)s:%(lineno)s - %(funcName)20s() ] %(message)s
datefmt=%H:%M:%S

[formatter_simple]
format=[%(asctime)s] [TARGETS] [%(levelname)9s] %(message)s
datefmt=%H:%M:%S

[handler_console]
level=DEBUG
class=StreamHandler
formatter=color
args=(sys.stdout,)

[handler_file]
level=INFO
class=handlers.TimedRotatingFileHandler
formatter=extended
args=('/tmp/targetservice.log', 'h', 1, 10)

[handler_syslog]
class=handlers.SysLogHandler
level=ERROR
formatter=extended
args=(('localhost', handlers.SYSLOG_UDP_PORT), handlers.SysLogHandler.LOG_USER)
